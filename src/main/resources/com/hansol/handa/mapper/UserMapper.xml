<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hansol.handa.mapper.UserMapper">
	
	<select id="read" resultType="UserVO">
		select * from member where member_id = #{username}
	</select>
	
	<insert id="register">
		INSERT INTO member 
			(member_id, password, member_name, member_call, 
			e_mail, gender, affiliates_id, position_id, certify_token)
		VALUES
			(#{member_id}, #{password},#{member_name}, #{member_call},
			#{e_mail}, #{gender}, #{affiliates_id}, #{position_id}, #{certifyToken})
	</insert>
	
	<update id="update">
		UPDATE 
			member 
		SET
			member_call = #{member_call},
			e_mail = #{e_mail},
			affiliates_id= #{affiliates_id},
			position_id= #{position_id},
			profile_name = #{profile_name},
			profile_uuid = #{profile_uuid},
			profile_path = #{profile_path}
		WHERE 
			member_id = #{member_id}
	</update>
	
	<update id="updateAuth">
		UPDATE 
			member 
		SET
			auth = #{auth}
		WHERE 
			member_id = #{member_id}
	</update>
	
	<select id="joinMembers" resultType="UserVO">
		SELECT * FROM member 
		WHERE member_id IN
		(SELECT member_id FROM challenge_join WHERE challenge_id=#{challenge_id})
	</select>
	
	<update id="updateToken">
		UPDATE 
			member 
		SET
			certify_token = #{certify_token}
		WHERE 
			member_id = #{member_id}
	</update>
	
</mapper>